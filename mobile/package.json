{
  "name": "claude-comm-mobile",
  "version": "1.0.0",
  "description": "claude-comm — application mobile native (relais Node embarqué + dashboard)",
  "private": true,
  "license": "MIT",
  "scripts": {
    "assets": "node copy-assets.js",
    "sync": "npm run assets && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.7.8",
    "capacitor-nodejs": "github:hampoelz/Capacitor-NodeJS#v1.0.0-beta.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8"
  }
}
